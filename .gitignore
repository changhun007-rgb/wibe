node_modules
dist
.wrangler
.dev.vars
.env
.env.local
*.log

# Design bundle (reference only — not part of the deployed site)
_design/

# Per-user Claude Code local settings (not portable, not for sharing)
.claude/
